```
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  let total
  if (quantidade <12) {
    total =   1.30*quantidade
  } else  { 
    total =   1*quantidade
    
  }
  return total
}
```