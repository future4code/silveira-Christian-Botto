import React from "react";

const  Header = (props) => {

    return(
        <div>
            {/* onClick={() => this.props.choosePage("azul")}>Home</button> */}
        </div>
    )
}
   

export default Header;